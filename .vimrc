execute pathogen#infect()
syntax on
filetype plugin indent on
set ruler
set guifont=Monaco:h11

" Use spaces instead of tabs
set expandtab

" Be smart when using tabs ;)
set smarttab

" 1 tab == 4 spaces
set shiftwidth=4
set tabstop=4

set ai "Auto indent
set si "Smart indent
set wrap "Wrap lines on display (not in file) (alternative is only part of the line is displayed, and you must scroll instead)
